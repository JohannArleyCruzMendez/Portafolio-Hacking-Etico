# Práctica 2.1: Enumeración con Nmap

## 🎯 Objetivo
Realizar un reconocimiento activo sobre un objetivo en la DMZ (`10.6.6.23`) para identificar servicios vulnerables y vectores de ataque potenciales mediante el uso de Nmap.

## 📝 Desarrollo de la Práctica

### 1. Descubrimiento de Hosts
Se realizó un barrido de ping para identificar máquinas activas en la red.
* **Comando:** `nmap -sn 10.6.6.0/24`
* **Resultado:** Se identificaron 7 hosts activos, incluyendo el objetivo **Gravemind (10.6.6.23)**.

### 2. Escaneo de Puertos y Servicios
Se ejecutó un escaneo para detectar puertos abiertos, versiones de servicios y Sistema Operativo.
* **Comando:** `nmap -v -p21,22,53,80,139,445 -sV -O 10.6.6.23`
* **Hallazgos Principales:**
    * **SO:** Linux (Kernel 4.x/5.x).
    * **Puerto 21 (FTP):** vsftpd 3.0.3.
    * **Puerto 22 (SSH):** OpenSSH.
    * **Puertos 139/445 (SMB):** Samba 4.9.5-Debian.

### 3. Enumeración de Vulnerabilidades (Scripts NSE)

#### 📂 FTP (Puerto 21)
Se detectó una configuración insegura de **Anonymous Login**.
* **Comando:** `nmap -p21 -sV -A 10.6.6.23`
* **Evidencia:** Acceso permitido como usuario `anonymous`.
* **Archivos expuestos:** `file1.txt`, `file2.txt`, `file3.txt` y un archivo crítico llamado **`supersecretfile.txt`**.

#### 🖧 SMB (Samba)
Se enumeraron usuarios y recursos compartidos utilizando scripts de Nmap.
* **Comando:** `nmap --script smb-enum-users,smb-enum-shares -p139,445 10.6.6.23`
* **Información Recolectada:**
    * **Nombre NetBIOS:** GRAVEMIND.
    * **Usuarios:** `masterchief` (RID 1000), `arbiter`.
    * **Recursos Ocultos:** `IPC$`, `print$`.
* **Vulnerabilidad Crítica:** El recurso compartido **`workfiles`** permite acceso de **LECTURA/ESCRITURA** a usuarios anónimos (Guest), lo que representa un riesgo alto de fuga de información o infección.

## 🚀 Conclusiones
El host `10.6.6.23` presenta vulnerabilidades graves de configuración. La combinación de acceso anónimo en FTP y permisos de escritura en SMB permite a un atacante no autenticado exfiltrar datos sensibles o comprometer la integridad del sistema.
